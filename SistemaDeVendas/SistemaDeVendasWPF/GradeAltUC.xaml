<UserControl x:Class="SistemaDeVendasWPF.GradeAltUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SistemaDeVendasWPF"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
        <ColumnDefinition/>
        <ColumnDefinition/>
    </Grid.ColumnDefinitions>

    <TextBlock Grid.Row="0">Quantidade:</TextBlock>
    <TextBlock Grid.Row="1">Tamanho:</TextBlock>
    <TextBox Grid.Row="0" Grid.Column="1" x:Name="NomeTxtBox"
                 Text="{Binding Grade.Quantidade}"></TextBox>
    <TextBox Grid.Column="1" Grid.Row="1" x:Name="CpdTextBox"
                 Text="{Binding Grade.Tamanho}"></TextBox>

    </Grid>
</UserControl>
